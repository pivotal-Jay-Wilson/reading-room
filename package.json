{
  "dependencies": {
    "@types/google-apps-script": "^1.0.45"
  },
  "name": "product-upgrade---google-",
  "version": "1.0.0",
  "main": "index.js",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.5.5"
  },
  "scripts": {
    "build": "rimraf build && tsc && cp appsscript.json build/appsscript.json && cp ./src/index.html ./build/index.html && cp ./src/index2.html ./build/index2.html && clasp push",
    "compile": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
